---
description: Plan Waymark work from a design document or feature idea using GitHub Issues
argument-hint: "<design-path-or-feature>"
---

Plan Waymark work for `$1` using the configured `github-issues` state backend from `AGENTS.md`; do not ask for the backend.

Before writing: read `AGENTS.md`, the dated design document, `docs/design/waymark-pi-project-context.md`, and the orchestration guidance at `X:\source\ai-engineering-orchestration\main\instructions\TEAM-ORCHESTRATION.md` and `TOOL-PI.md` when available. For parity work, audit `X:\source\sable\main` and produce a source-delta matrix with exact paths. Do not implement code.

Use `/inspire` for unresolved product possibilities and `/brainstorm` for focused clarification and alternatives before finalizing the sprint. The authoritative GitHub issue must include scope, Contract Impact Check, provider/API/CLI/artifact/database-safety decisions, implementation-ready tasks, verification commands, quality gates, and original acceptance criteria. Use `.pi/tmp/` for issue-body drafts. Never close issues or apply final disposition labels.

End with artifact locations, unresolved questions, and whether the sprint is ready for human approval.
